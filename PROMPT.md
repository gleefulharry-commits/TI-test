# Kimi Agent 指令：帮用户生成并部署人格测试网站

> 当你（Kimi Agent）在浏览器里看到这条指令时，说明用户想让你基于一个现成的人格测试 HTML 框架，帮他自动生成一个完整的、可部署的网站。

## 第一步：克隆框架仓库

打开终端或命令面板，执行：
```bash
git clone https://github.com/gleefulharry-commits/TI-test.git
```

框架核心文件只有 `TI-test/index.html`，这是一个单文件 HTML，内嵌了 CSS + JS + 算法骨架。

## 第二步：读取用户的需求

用户会告诉你他想做什么风格的人格测试，通常包括：
- 主题（例如：职场人格、恋爱人格、学生党人格、MBTI变体）
- 题目数量（一般 12~16 道）
- 人格数量（一般 8~12 个基础 + 3~5 个隐藏）
- 调性（搞笑/治愈/毒舌/文艺/暗黑）
- 是否希望有隐藏人格彩蛋

如果用户没有明确说明，你应主动问他 1~2 个关键问题来确认方向，不要假设。

## 第三步：修改框架内容

用代码编辑器打开 `TI-test/index.html`，根据用户要求填充以下 5 个【自定义区域】。

### 区域 1：题目库（`const questions`）
- 替换所有中文占位符为实际题目和选项
- 每道题 4~5 个选项，每个选项分配合理的 `scores`（维度得分）
- 维度名保持一致（例如：外向、内向、秩序、随性、稳定、敏感、好奇、佛系等）
- 建议加入 1~2 道"癫题"（奇葩选项）增加 viral 感

### 区域 2：基础人格库（`const personalities`）
- 写 8~12 个抓马有网感的人格名称
- 每个人格写一段判词式描述 + 一句金句 tagline
- `dimensions` 必须与题目库里的维度名对应，否则匹配会失效

### 区域 3：隐藏人格库（`const hiddenPersonalities`）
- 设计 3~5 个隐藏人格
- 分配稀有度：rare / superRare / legendary
- 写 `condition` 函数，用特定题目的选项组合触发
- 建议 legendary 触发条件最难，增加稀缺感

### 区域 4：癫题加成（可选）
- 如果有特别炸裂的选项，可以给它额外加分
- 例如：某题选 E（"加辣椒到奶茶里"）→ 破坏力 +5

### 区域 5：强制规则（可选）
- 当某维度得分过高时，强制显示特定人格

### 同时修改首页 UI
- 把 `【品牌名】`、`【副标题 Slogan】` 改成用户提供的名字
- 如果想加背景图，可以在 `<style>` 里给 `.home-screen` 加 `background-image`

## 第四步：本地验证

保存文件后，用浏览器打开 `TI-test/index.html`，自己做一遍测试，确认：
1. 所有题目正常显示，选项能点击
2. 结果页能正常出现人格名称和描述
3. 隐藏人格触发条件正确
4. 没有明显的 console 报错

## 第五步：帮助用户部署

用户通常不会部署，你需要帮他把网站上线。

### 方法 A：GitHub + Vercel（推荐）

1. 帮用户把修改后的代码推送到他自己的 GitHub 仓库（如果他给了仓库地址），或者原地推回 `TI-test`
2. 打开 `https://vercel.com/new`
3. 导入该 GitHub 仓库
4. Framework Preset 保持默认（Static / Other）即可，因为这是单 HTML 文件
5. Root Directory 填 `/`
6. 点击 Deploy
7. 部署成功后，把生成的 `xxx.vercel.app` 域名复制给用户

### 方法 B：GitHub Pages

1. 把 `index.html` 推到 GitHub 仓库
2. 进入仓库 Settings → Pages
3. Source 选择 Deploy from a branch，Branch 选 main / root
4. 等 1 分钟，把 `xxx.github.io/xxx` 链接发给用户

### 方法 C：Netlify Drop

1. 打开 `https://app.netlify.com/drop`
2. 把 `TI-test` 文件夹直接拖进去
3. 复制生成的 `xxx.netlify.app` 链接发给用户

## 注意事项

- **不要删除框架的算法部分**，只改数据和文案
- **确保维度名拼写一致**，题目库、人格库、癫题加成里的维度名必须完全一致
- **如果用户没有图片**，结果页用默认的 emoji 占位符即可，不用到处找图
- **稀有度颜色已内置**：稀有=蓝色，超稀有=紫色，传说=金色
- 整个网站是**纯前端**，不需要后端、数据库或 API

## 输出格式

完成部署后，请给用户一个简洁的总结：
1. 网站链接
2. 一共做了多少道题、多少个人格
3. 隐藏人格的触发彩蛋提示（1~2 个）
4. 告诉他如果想改内容，只需要改 `index.html` 里的 5 个自定义区域
